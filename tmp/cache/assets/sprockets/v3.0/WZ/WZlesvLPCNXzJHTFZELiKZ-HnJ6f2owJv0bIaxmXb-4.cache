I"ç(function() {
  var bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; },
    extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    hasProp = {}.hasOwnProperty;

  AuditReports.Models.AuditReportSummary = (function(superClass) {
    extend(AuditReportSummary, superClass);

    function AuditReportSummary() {
      this._listenToChannel = bind(this._listenToChannel, this);
      return AuditReportSummary.__super__.constructor.apply(this, arguments);
    }

    AuditReportSummary.prototype.relations = [
      {
        type: Backbone.HasMany,
        key: 'measure_summaries',
        relatedModel: 'AuditReports.Models.MeasureSummary',
        reverseRelation: {
          key: 'measure_summary'
        }
      }
    ];

    AuditReportSummary.prototype.defaults = {
      id: '',
      annual_cost_savings: null,
      annual_energy_savings: null,
      annual_water_savings: null,
      years_to_payback: null,
      cost_of_measure: null,
      utility_rebate: null
    };

    AuditReportSummary.prototype.initialize = function() {
      return AuditReports.EventBus.on("channel:audit_report_summary:" + (this.get('id')), this._listenToChannel);
    };

    AuditReportSummary.prototype._listenToChannel = function(args) {
      var data, event;
      event = args[0], data = args[1];
      if (event === 'change') {
        return this.set(data);
      }
    };

    return AuditReportSummary;

  })(Backbone.RelationalModel);

  AuditReports.Models.AuditReportSummary.setup();

}).call(this);
:ET
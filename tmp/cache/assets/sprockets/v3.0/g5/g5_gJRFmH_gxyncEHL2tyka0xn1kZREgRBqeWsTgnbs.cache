I"É(function() {
  var bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; },
    extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    hasProp = {}.hasOwnProperty;

  AuditReports.Models.OriginalStructureFieldValue = (function(superClass) {
    extend(OriginalStructureFieldValue, superClass);

    function OriginalStructureFieldValue() {
      this._listenForOriginalStructureChange = bind(this._listenForOriginalStructureChange, this);
      this._listenForEffectiveStructureChange = bind(this._listenForEffectiveStructureChange, this);
      return OriginalStructureFieldValue.__super__.constructor.apply(this, arguments);
    }

    OriginalStructureFieldValue.prototype.defaults = {
      id: '',
      real_id: '',
      name: '',
      api_name: '',
      effective_value: '',
      value: '',
      value_type: '',
      from_audit: false
    };

    OriginalStructureFieldValue.prototype.initialize = function() {
      this.listenTo(AuditReports.EventBus, "channel:effective_structure_field_values", this._listenForEffectiveStructureChange);
      return this.listenTo(AuditReports.EventBus, "channel:original_structure_field_value:" + (this.get('real_id')), this._listenForOriginalStructureChange);
    };

    OriginalStructureFieldValue.prototype._listenForEffectiveStructureChange = function(args) {
      var data, effectiveValue, effectiveValues, event;
      event = args[0], data = args[1];
      effectiveValues = data[this.get('measure_selection_id')][this.get('structure_wegoaudit_id')];
      effectiveValue = effectiveValues[this.get('api_name')];
      return this.set('effective_value', effectiveValue);
    };

    OriginalStructureFieldValue.prototype._listenForOriginalStructureChange = function(args) {
      var data, event;
      event = args[0], data = args[1];
      return this.set('value', data);
    };

    return OriginalStructureFieldValue;

  })(Backbone.RelationalModel);

  AuditReports.Models.OriginalStructureFieldValue.setup();

}).call(this);
:ET
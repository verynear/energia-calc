<%- title @audit.name %>

<%- content_for(:crumbs) do %>
  <div class="crumbs">
    <%= link_to 'All audits', audits_path %>
    <i class="icon-chevron-right"></i>
    <%= @audit.name %>
  </div>
<% end -%>

<%- content_for(:title_links) do %>
  <%= lock_icon(current_audit) %>
<% end -%>

<%= render 'audits_menu' %>

<div class="active-content js-active-content">
  <%= render 'tab_menu' %>

  <div class="pane-wrapper">
    <%= render 'web/audit_structures/substructures', audit_structure: @audit.audit_structure %>
    <%= render 'web/photos/photos', audit_structure: @audit.audit_structure %>
    <%= render 'web/audit_structures/details', audit_structure: @audit.audit_structure %>
    <%= render 'utility_data', audit_structure: @audit.audit_structure %>
    <%= render 'measures', audit: @audit %>
  </div>
</div>

<%= render 'web/audit_structures/clone_structure_modal' %>
<%= render 'web/audit_structures/link_structure_modal' %>
<%= render 'web/audit_structures/new_structure_modal',
      parent_structure: @audit.audit_structure %>
<%= render 'web/sample_groups/new_sample_group_modal',
      parent_structure: @audit.audit_structure %>
